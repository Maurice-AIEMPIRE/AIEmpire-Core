# Core dependencies for AI Empire
aiohttp>=3.9.0           # Async HTTP requests
pyyaml>=6.0              # YAML parsing for task configs
python-dotenv>=1.0.0     # Environment variables from .env

# Optional but recommended
requests>=2.31.0         # Synchronous HTTP requests
